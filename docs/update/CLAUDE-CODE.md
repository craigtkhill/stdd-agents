# Update STDD Agents - Claude Code

Run these commands from your **project root**.

```bash
cd .claude
git pull origin main
cd ..
git add .claude
git commit -m "chore: update stdd-agents submodule"
```
